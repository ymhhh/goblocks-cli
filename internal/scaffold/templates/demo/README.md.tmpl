# {{.ServiceName}}

Demo service generated by [goblocks](https://github.com/ymhhh/goblocks).

## 目录结构

* `core` — 数据模型（实体）
* `domain` — 仓储接口（端口）
* `handlers` — 应用层用例（业务编排）
* `infrastructure` — 组合根（Composition Root），依赖注入与启动

依赖方向：`handlers → domain → core`，`infrastructure` 组装各层，`main` 仅引导启动。

参考：[ddd-onion-sample](https://github.com/ymhhh/ddd-onion-sample)

## 运行

```bash
go mod tidy
go run .
```

## API

- `GET /health` — 存活探针（框架 `server.http.health`）
- `GET /ready` — 就绪探针
- `GET /users/:id` — 获取用户（L2 每用户配额 + L3 路由限流，见 `config` 中 `user` / `routes`）

{{- if .WithAI}}

- `POST /ai/chat` — AI 对话（需配置 `OPENAI_API_KEY`）
{{- end}}

## 配置

编辑 `config/config.yaml`，或通过环境变量覆盖（`GOBLOCKS_*` 前缀）。

{{- if .WithGRPC}}

## gRPC

gRPC 健康检查已注册在配置的 `grpc.addr` 端口。自定义 proto 见 `proto/` 目录。
{{- end}}
