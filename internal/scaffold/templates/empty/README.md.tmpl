# {{.ServiceName}}

Service generated by [goblocks](https://github.com/ymhhh/goblocks).

## 目录结构

* `core` — 数据模型（实体）
* `domain` — 仓储接口（端口）
* `handlers` — 应用层用例（业务编排）
* `infrastructure` — 组合根（Composition Root），依赖注入与启动

依赖方向：`handlers → domain → core`，`infrastructure` 组装各层，`main` 仅引导启动。

## 运行

```bash
go mod tidy
go run .
```

## 配置

编辑 `config/config.yaml`，或通过环境变量覆盖（`GOBLOCKS_*` 前缀）。
